services:
  dashboard:
    build:
      context: .
      dockerfile: dashboard_v2/Dockerfile
    ports:
      - "${ASE_HOST_PORT:-8080}:8080"
    environment:
      ASE_DASHBOARD_PORT: "8080"
      ASF_AUDIT_DB: /data/asf_local.db
      ASF_DASHBOARD_READONLY: "1"
    volumes:
      - ${ASF_AUDIT_DB_HOST}:/data/asf_local.db:ro
    restart: unless-stopped
