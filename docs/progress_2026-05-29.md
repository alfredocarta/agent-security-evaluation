# ASF Progress Notes — 2026-05-29

## Context

Work completed after the 2026-05-28 Claudio call. Two main tracks:
benchmarking reliability fixes and a full multi-round code review of
hardening.py and interceptor.py with targeted fixes in every round.

---

## Benchmark Fixes

### 1. Agent suspension contamination bug (benchmark_deepset.py)

Root cause: `registry.suspend_agent()` is called by the interceptor on every
DENY verdict. The deepset benchmark reinstated the agent only once at module
load, so after the first DENY the agent stayed suspended and all subsequent
samples across all configs returned DENY immediately — making recall and FPR
meaningless for all pipeline configs.

Fix: `registry.reinstate_agent("benchmark-agent")` added before each sample
in `run_config`. All other benchmark scripts already had per-call reinstatement
inside their `classify_*` functions. Only deepset was missing it.

Impact on deepset results:
- Stage 1+2+2.5: recall 3.4% → 5.7% (before additional pattern fixes)
- Always-Stage25: recall/FPR went from the corrupted 100%/99% to the correct 99.2%/1.3%

### 2. SPML benchmark (new)

Added `SPML_LIMIT` env var and `classify_union` to `benchmark_spml.py`.
Fixed double-finalize crash in by-degree save. Added Always-Stage25 behind
`SPML_ALWAYS25=true` opt-in to prevent unintended long runs.

SPML results (2000-sample cap, 1000 inj / 1000 benign):

| Config | Recall | FPR |
|---|---|---|
| ASF Stage 1+2+2.5 | 13.1% | 0.0% |
| ONNX PG86M | 52.5% | 0.0% |
| Union | 52.9% | 0.0% |

### 3. Benchmark anomaly investigations

**Jackhhao 100% recall / 96% FPR**: the benign class contains creative roleplay
prompts ("You are a medieval blacksmith") that use identical language to
jailbreak attacks. Stage 2 TF-IDF cannot distinguish them. ONNX handles it
(0.2% FPR) because it was trained on this distinction. Not a methodology bug.

**Safeguard 60% FPR**: the benign class is instruction-format AI assistant
prompts. L1.5 instruction_lang heuristic fires at fast-path BLOCK. All configs
show identical FPR because Stage 2 never sees these samples. Not a methodology
bug — ASF is calibrated for agentic tool context, not general instruction
benchmarks.

---

## Code Review — Hardening.py and Interceptor.py

Seven rounds of Codex review with targeted fixes after each round.

### Round 1 — Pipeline correctness

- **_RE_DOC_BULLET typo**: `\^\d+` matched a literal caret, not a line start.
  Numbered markdown lists never contributed to document context detection.
  Fixed: `\^` → `^`.
- **VT/FF in ZERO_WIDTH_RANGES**: `(0x000B, 0x000C)` included form feed and
  vertical tab, legitimately present in PDF-extracted documents. Removed.
- **Stage 3 audit log**: when `ASF_DISABLE_STAGE25=true`, the audit event
  still said "DeBERTa uncertain". Fixed: separate `STAGE_2.5_SKIPPED` event.
- **`__import__("sys").stderr`**: inconsistent with rest of file. Fixed to
  `sys.stderr` throughout.

### Round 2 — Semantic probe expansion (interceptor.py)

Probe expanded from 12 to 27 patterns across multiple rounds:
- Tightened "you are now": requires a role/persona noun within 30 chars
- Broadened "act as": now "act as/like" with explicit AI/persona target list
- Added: system prompt exfiltration, developer/jailbreak mode activation,
  role-token injection (`<|im_start|>`, `[INST]`, `<<SYS>>`), simulate
  unrestricted AI mode, disable safety/guardrails, authority spoofing
  (`SYSTEM: ... ignore`), "ignore all above/safety policy/system message",
  override developer message, indirect identity assignment, fictional framing,
  praise+pivot, German injection patterns, translation exploitation

### Round 3 — Known payload patterns (hardening.py)

- **Lowercasing bug**: `_detect_known_payloads` ran all patterns against
  `text.lower()` but several patterns required uppercase (`[A-Z][a-z]+`,
  `DAN`, `DUDE`) and therefore never matched. Fixed: all patterns converted
  to `re.IGNORECASE` and run against original text.
- **Score calibration**: "your new/real task" raised 0.5→0.65, "disregard
  previous" raised 0.5→0.65, "ignore previous instructions" raised 0.5→0.75
  (now critical path).
- **`drop\\s+table` bug**: double backslash in raw string matched literal
  `drop\s+table`. Fixed to `r'drop\s+table'` with `re.IGNORECASE`.
- **im_sep**: added to `_RE_STRUCTURAL_CHAT_TOKEN` for parity with
  interceptor.py role-token pattern.
- **German pattern fixes**: vergiss now requires `vorherigen/bisherigen/deine`
  + `anweisungen`; stell dir vor now requires `du bist`.
- **Identity assignment**: removed broken `[A-Z][a-z]+` capitalization
  heuristic, replaced with explicit AI/persona keyword list.

### Round 4 — Zero-width character tiers (hardening.py)

`ZERO_WIDTH_CHARS` split into two sets:

- **Critical (weight 1.0)**: Zero Width Space, Word Joiner, Soft Hyphen,
  BOM mid-text, bidi overrides LRE/RLE/PDF/LRO/RLO, bidi isolates
  LRI/RLI/FSI/PDI. These appear only in adversarial text.
- **Soft (weight 0.3)**: ZWNJ, ZWJ, LRM, RLM, ALM, Hangul fillers, Khmer
  inherents. These appear in legitimate Arabic/Hebrew/Persian/Indic/emoji text.
- **Variation selectors FE00-FE0F**: moved from critical ZERO_WIDTH_RANGES to
  new `ZERO_WIDTH_RANGES_SOFT` (0.3). Emoji using text/emoji presentation
  selectors (☎︎ ❤️) no longer hard-block.
- **`_strip_zero_width`**: now only strips critical chars. Soft chars are left
  in place — stripping ZWNJ/ZWJ breaks Persian text and emoji composition.

### Round 5 — Score accumulation guard (hardening.py)

The multi-signal boost (`score * (1 + 0.3 * (active - 1))`) could push benign
technical fixtures (README with structural markers, base64 examples, mixed
scripts, command patterns) past the 0.6 block threshold without any direct
attack evidence.

Fix: boost only activates when a strong anchor signal is present:
`known_payloads > 0` OR `zero_width > 0` OR `sensitive_file_abuse > 0`.
Pure accumulation of structural/encoding/language signals without payload
evidence no longer triggers a block.

### Round 6 — Base64, unicode, decode size (hardening.py)

- **Base64 keyword list**: was generic words (`system`, `print`, `output`)
  that fire on JWT tokens, code examples, and API documentation. Narrowed to
  specific attack phrases (`ignore previous instructions`, `act as dan`,
  `jailbreak`, `developer mode`, etc.). Max score capped at 0.65 — base64
  alone can no longer critical-block.
- **`_detect_unicode_anomalies`**: Latin+Cyrillic score reduced 0.8→0.5,
  Latin+Greek reduced 0.5→0.4. Neither crosses the critical threshold (0.7).
  Ukrainian users mentioning English brand names or API names no longer
  hard-block.
- **Decode size cap**: 512 KB limit added per layer in `decode_and_rescan` to
  prevent memory/CPU pressure from large encoded payloads.

### Round 7 — ROT13, instruction_lang, sensitive paths, doc dampener (hardening.py)

- **ROT13 keywords**: narrowed to the same attack-phrase list as base64,
  max 0.65 (consistent treatment).
- **`instruction_lang` cap**: `min(count * 0.3, 1.0)` → `min(count * 0.3, 0.65)`.
  Legitimate developer tool calls (open file, run command, send output, print
  result) can innocuously match 3-4 patterns. Capped so instruction_lang alone
  can never critical-block.
- **Sensitive file paths expanded**: added `/proc/self/environ`,
  `~/.aws/credentials`, `.env`, `/etc/hosts`, `/var/run/secrets` (Kubernetes
  tokens), `id_rsa.pub`. Also added `type`/`get` to read verbs and `post`/`fetch`
  to exfil verbs.
- **Doc dampener extended**: guard changed `>= 0.6` → `> 0.6` so two-pattern
  instruction_lang (score 0.6) is still dampened in document context. Dampening
  extended to `structural`, `base64`, and `rot13` in addition to
  `instruction_lang` and `entropy`.

### Round 8 — FP-prone payload cleanup (hardening.py)

Removed patterns that duplicated interceptor.py probe coverage and added
FP risk at the L1.5 block level (where direct blocks happen vs the probe
which only escalates to DeBERTa):
- Removed: `override all instructions` (→ interceptor.py:28)
- Removed: `reveal/show instructions`, `show system prompt` (→ interceptor.py:35)
- Narrowed: `prompt is` → requires exfiltration verb prefix

Removed unused threshold constants `_HEURISTIC_BLOCK`, `_HEURISTIC_CLEAR`,
`_SOFT_THRESHOLD` from hardening.py — `apply_l1_5_hardening` uses
`_DEFAULT_THRESHOLD=0.6`, not these constants.

---

## Benchmark Results (final state, 2026-05-29)

### deepset — 662 samples (263 injection, 399 benign)

| Configuration | Recall | FPR |
|---|---|---|
| Sigil heuristic v2 (peer baseline) | 21.3% | 0.0% |
| ASF L1.5 only | 5.7% | 0.5% |
| ASF Stage 1+2 | 19.4% | 1.0% |
| ASF Stage 1+2+2.5 | 19.4% | 0.7% |
| ASF Always-Stage25 | 99.2% | 1.3% |
| ONNX Prompt Guard 86M | 22.8% | 0.3% |
| ASF L1.5 + ONNX (union) | 24.3% | 0.8% |

Before vs after code review: Stage 1+2+2.5 went from 5.7% → 19.4% recall
with FPR from 0.5% → 0.7% (+2 FPs out of 399 benign). The known payload
pattern fixes (lowercasing bug, score calibration) drove the improvement.

---

## Open Items

1. Mindgard/BIPIA full runs with Always-Stage25 and union rows — hardware-gated
2. Dashboard v2 DB lock contention — mcp_server + hook_daemon hold asf_local.db
3. OPI re-run at 5000 samples to validate results vs old 44% baseline
   (2000-sample subset showed different distribution, not comparable)
4. bipia Full pipeline anomaly — Full pipeline = L1.5 only, unexplained without
   re-run with current code
5. Re-enable ASF hook after benchmarks (`cp ~/.claude/settings.json.bak ~/.claude/settings.json`)
